streamlit>=1.50
plotly>=5.20

# backend (formerly the standalone FastAPI service — now called in-process)
fastapi>=0.115
pydantic>=2.7
pydantic-settings>=2.4
httpx>=0.27
structlog>=24.1
python-dotenv>=1.0

# only needed if you set OPENAI_API_KEY in Secrets (optional — falls back
# to a deterministic, rule-based recommendation engine if not installed
# or not configured)
openai>=1.40
